# Node on Ubuntu Latest

name: Node Ubuntu Latest

on:
  push:
    branches:
      - '*'

jobs:
  build-node:
    runs-on: ubuntu-latest
    steps:
      - name: path before
        run: pwd | ls
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: path after
        run: pwd | ls
      - name: om
        uses: actions/setup-node@v3
        with:
          node-version: '12'
      - run: npm install
      - run: npm start

